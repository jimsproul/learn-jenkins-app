 pipeline {
    agent any

    stages {
        // Start with a build

        stage('Docker') {
            steps {
                sh 'docker build -t my-playwright .' 
            }
        }
    }
 }